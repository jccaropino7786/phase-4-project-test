import React from "react";
import { IUserState } from "./user-context";
export interface IOptions {
    reLogin(): Promise<IUserState>;
}
declare const UserProvider: React.FC<IOptions>;
export default UserProvider;
